# 進捗

## 全体計画（設計書.md準拠）
1. YouTube再生位置の保存/復元ロジックを安定化する
2. ポップアップ機能（一覧/検索/既存タブ切替）を安定化する
3. 権限と実装の整合を保ち、不要権限を削減する
4. CHANGELOG/README/バージョンを各リリースで同期する

## 現在の立ち位置
- フェーズ：v1.6.1 バグ修正リリース
- いまやっていること：バージョン更新と修正差分の記録、コミット準備
- 次にやること：差分確認後、1目的でコミット

## 完了したタスク
- 2026-02-16: v1.6.1 向けに `manifest.json` のバージョンを 1.6.1 に更新
- 2026-02-16: `README.md` の現在バージョン表記を 1.6.1 に更新
- 2026-02-16: `CHANGELOG.md` に 1.6.1（復元競合修正、既存タブ切替権限修正、注入ロジック無効化）を追記

## バグ / エラー修正ログ
- 2026-02-16: 再起動直後に保存済み位置より先に新規位置が保存される競合を修正
  - 現象：保存済み位置が 0秒付近で上書きされる場合がある
  - 発生条件：再起動直後の復元時、pause イベント経由の保存が先行
  - 原因：復元前 `video.pause()` で発火した保存処理が `isRestoring` ガード前に走る
  - 対策：復元前に `isRestoring=true` をセットし、復元しない分岐でも確実にフラグ解除
  - 再発防止（任意）：復元開始前後の保存ガードを維持

## 未着手 / 保留
- `設計書.md` が未配置のため、仕様一次情報はユーザー指示と実装差分から運用中

## メモ / 課題
- Service Worker の再注入ロジックは `background.js` にバックアップコメントとして保持（現在は無効化）
- 既存タブ切替は `tabs` 権限を前提とするため、権限変更時は動作確認が必要
