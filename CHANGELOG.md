# YouTube 再生位置自動保存 - 更新履歴

## [1.8.2] - 2026-06-17

### 修正
- **拡張コンテキスト無効化時の古いインスタンス（orphan）の自己停止処理を追加**：拡張機能のリロードや更新によって新しいコンテンツスクリプトが再注入された後も、ページ内に残っていた古いスクリプトのタイマー処理やリスナーが動いたまま `chrome.storage` 等にアクセスしようとして「Extension context invalidated」エラーが発生する問題を修正。コンテキストが無効化されたことを検知した時点で、自身のタイマーやリスナーを自己停止（クリーンアップ）するようにしました。

---

## [1.8.1] - 2026-06-17

### 変更
- README を現行機能に合わせて更新し、保存間隔、復元後の自動再生、視聴完了時の自動削除、設定画面でのデータ管理について実装どおりの説明に整理しました。
- 既存 YouTube タブへの content script 再注入について、Service Worker の再注入タイミングと `content.js` 側の二重起動防止を README に追記しました。
- ファイル構成の説明を現行の `background.js` と `popup.js` の役割に合わせて更新しました。

---

## [1.8.0] - 2026-06-17

### 追加
- **「最後まで見た動画を自動削除」のON/OFF設定**：視聴完了時に保存データを削除する従来動作を、設定でオフにできるようにしました。オフにすると最後まで見た動画も一覧に残ります（「保存と復元」セクション）。
- **保存間隔の設定**：再生位置の保存間隔を 5秒 / 10秒 / 30秒 から選べるようにしました（「一覧とデータ」セクション、詳細設定向け）。設定変更時は保存中の間隔も即座に張り替わります。

### 改善
- 上記2項目を含む新しい設定キーは、既存のエクスポート/インポートおよび設定マージ処理にそのまま追従します。

---

## [1.7.0] - 2026-06-16

### 追加
- **詳細設定画面の追加**：ポップアップに設定画面を搭載し、以下の項目をカスタマイズ可能にしました。
  - 復元時の通知表示のON/OFF
  - 復元後の自動再生のON/OFF
  - 短時間（10秒、30秒、1分）だけ見た動画を保存しない設定
  - 古い保存データ（30日、90日、180日）の自動削除
  - 一覧から動画を開くときのタブ動作（既存タブ優先 または 常に新しいタブ）
- **データ管理機能**：保存した再生位置および設定データをJSON形式でエクスポートする機能、およびJSONファイルからデータをインポートする機能を追加しました。
- **動的再注入ロジックの再有効化**：拡張機能の更新・再読み込み時や、ブラウザ起動時、タブのアクティブ化・更新時に、既に開いているYouTubeタブへ content script を自動再注入するロジック（Service Workerによる動的注入）を再度有効化しました。

### 改善
- **多重注入防止とクリーンアップ**：再注入時にイベントリスナー等が重複して登録されるのを防ぐため、既存のコントローラ（`window.__ytPositionSaverController`）をクリーンアップしてから初期化する仕組みを構築しました。

---

## [1.6.5] - 2026-06-15

> 注: v1.6.5 の変更内容は履歴として残していますが、現在は v1.6.4 を使用しています。

### 修正
- **ライブ配信でリロード（Ctrl+R）した際に動画が一瞬再生されて一時停止してしまう問題を修正**：動画の読み込み直後（メタデータロード前）の極めて早い段階でライブ判定を行っていたため、プレイヤーUIが未構築で非ライブと誤判定され、古い保存データに基づいた不要な一時停止・復元が引き起こされていた。動画のメタデータが読み込まれたこと（`readyState >= 1`）を確認した後にライブ判定と復元処理を開始するように変更し、誤判定を根本解決。
- **ライブ配信の判定精度を大幅に向上**：SPA遷移後やリロード時のDOM構造の変更によるパース失敗を防ぐため、Main World に一時スクリプトを注入して `ytInitialPlayerResponse` の情報を直接・確実に取得する方式に変更。これにより「動画が一瞬再生されて一時停止してしまう」問題の根本原因であった「ライブ状態の誤判定による復元処理の誤発動」を防止。

---

## [1.6.4] - 2026-06-15

### 修正
- **DVR有効ライブ配信で動画が一時停止したままになる問題を修正**：DVRライブは `video.duration` が有限値になり従来の長さ判定をすり抜けていたため、プレイヤーの「ライブ」バッジ（`.ytp-live-badge`）の可視状態でライブを判定するよう変更
- content script の isolated world からは読めない `window.ytInitialPlayerResponse` ベースのライブ判定（実質無効なデッドコード）を廃止し、DOMベースの判定に一本化
- アーカイブ（配信終了後）は従来どおり保存・復元の対象として動作（ライブバッジが非表示のため自動的に対象内）

### 変更
- ライブ判定結果をキャッシュ化し、`timeupdate`（高頻度）での DOM 計測（`getBoundingClientRect` 等によるリフロー）を排除。判定は復元時・定期保存時・初期化時にのみ実行
- README を現在の v1.6.4 処理内容に合わせて更新

---

## [1.6.3] - 2026-06-15

### 修正
- **ライブ配信ページで動画が一時停止したままになる問題を修正**：アーカイブではないライブ配信を保存・復元対象外にし、復元前の `pause` が残らないように修正
- `duration` が `Infinity` / `null` などの無効な保存データを復元対象外とし、既存の無効データは検出時に削除

---

## [1.6.2] - 2026-02-18

### 修正
- **動画終了時の自動削除が効かない場合があるバグを修正**：動画が最後まで再生された後、SPA遷移時の `saveForNavigation` がデータを再保存してしまうレースコンディションを修正
- `hasEnded` フラグを追加し、`ended` イベント後の全保存処理をブロック
- 動画終了付近（残り3秒以内）でも保存データを自動削除（`ended` イベント未発火時のフォールバック）

---

## [1.6.1] - 2026-02-16

### 修正
- **再生位置復元の競合修正**：再起動直後に `pause` イベント経由で 0秒付近が先に保存され、既存の保存位置を上書きしてしまうケースを修正
- **既存タブ切替の権限修正**：`manifest.json` に `tabs` 権限を追加し、ポップアップからの同一動画タブ切替が正常動作するように修正

### 変更
- `background.js` の再注入処理を無効化し、バックアップコメントとして保持
- `manifest.json` から `scripting` / `host_permissions` を削除し、必要最小権限へ整理

---

## [1.6.0] - 2026-02-16

### 追加
- **既存タブ切替**：ポップアップから動画を開く際、同じ動画のタブが既に開かれていればそちらに切り替え（なければ新規タブ）
- **検索機能**：ポップアップに検索バーを追加。タイトルまたは動画IDでリアルタイム絞り込みが可能

### 変更
- `popup.html` に検索入力欄を追加
- `popup.css` に検索バーのスタイルを追加
- `popup.js` の `applyI18n` を `data-i18n-placeholder` 属性に対応
- i18n メッセージに `popupSearchPlaceholder` を追加（日本語・英語）

---

## [1.5.0] - 2026-02-16

### 追加
- **ブラウザ再起動対応**：Service Worker（`background.js`）を追加し、ブラウザ再起動時・拡張機能更新時に既存のYouTubeタブがアクティブになったタイミングで content script を自動再注入
- `content.js` に多重注入防止ガード（`window.__ytPositionSaverLoaded`）を追加

### 変更
- `manifest.json` に `scripting` 権限と `background.service_worker` を追加

---

## [1.4.1] - 2026-02-16

### 改善
- 例外処理の空 `catch` を `console.warn` に置換し、障害原因の追跡性を向上
- `savePositionCore` を `async` 化し、`chrome.storage.local.set` を `await` で保存完了を保証
- `loadedmetadata` コールバック内でクロージャ変数（`capturedVideoId`）を使用し、急速連続遷移時のコンテキスト混線を防止
- `video.play()` 失敗時にエラー理由をログ出力するように変更

---

## [1.4.0] - 2026-02-16

### 追加
- **ポップアップUI**：ツールバーアイコンから保存済み動画の一覧表示・個別削除・一括削除・有効/無効切替が可能に
- **多言語対応（i18n）**：日本語（デフォルト）・英語に対応。ブラウザの言語設定に自動追従
- `_locales/ja/messages.json`、`_locales/en/messages.json` を追加
- `popup.html`、`popup.js`、`popup.css` を追加

### 修正
- `seeked` イベントにタイムアウト（3秒）を追加し、イベントが発火しない場合でもフォールバックで復元処理を続行するように修正（Promise の永久ブロックを防止）

### 変更
- `manifest.json` の `name` / `description` を `__MSG_*__` 形式に変更（i18n対応）
- `manifest.json` に `default_locale`、`action.default_popup` を追加

---

## [1.3.1] - 2025-12-14

### 修正
- 戻る/進む（popstate）などで `init()` が二重実行されやすいケースを抑止し、遷移時保存を安定化

## [1.3.0] - 2025-12-14

### 変更
- READMEの内容を公開向けに整理（機能説明、権限、保存データ、ストア掲載用説明文を追加）
- バージョン表記を `manifest.json`（1.3.0）に合わせて整合

## [1.2.0] - 2025-11-26

### 追加
- 復元中フラグ（`isRestoring`）によるイベント競合防止
- 動画要素検出時の即時一時停止（最初の数秒再生を防止）
- シーク完了を Promise で確実に待機

### 改善
- マジックナンバーを定数化（`VIDEO_CHECK_INTERVAL_MS`, `NOTIFICATION_DURATION_MS` など）
- `videoCheckIntervalId` のクリーンアップ漏れを修正
- イベントリスナーの適切な削除（メモリリーク防止）
- `yt-navigate-finish` イベント使用（MutationObserver より効率的）
- `async/await` による競合状態の解消

---

## [1.1.0] - 2025-11-26

### 追加
- `timeupdate` イベントで常に最新の再生位置を追跡
- 戻るボタン押下時の再生位置保存
- 無効な値（0秒/NaN秒）の保存防止

### 改善
- 一時停止中の重複保存を防止
- `Extension context invalidated` エラーのハンドリング

---

## [1.0.0] - 2025-11-26

### 機能
- YouTube 動画の再生位置を自動保存
- 5秒ごとの定期保存
- ページ再読み込み時に復元
- 再訪問時に復元
- 戻る/進むボタンでの復元
- 動画終了時にデータ削除
- 動画の長さチェック（差異が5秒以上の場合はスキップ）
- 復元時のトースト通知表示

### 保存トリガー
- 5秒ごとの定期保存
- 一時停止時
- ページ遷移時
- タブ切り替え時
- ページ離脱時

---

## 技術仕様

### 使用 API
- `chrome.storage.local` - 再生位置の保存
- `chrome.runtime.id` - 拡張機能の有効性チェック
- `chrome.tabs.query` - 既存YouTubeタブの検索（ポップアップ既存タブ切替）

### イベント
- `yt-navigate-finish` - YouTube SPA ナビゲーション検出
- `popstate` - 戻る/進むボタン検出
- `visibilitychange` - タブ切り替え検出
- `beforeunload` - ページ離脱検出
- `timeupdate` - 再生位置追跡
- `pause` - 一時停止検出
- `ended` - 動画終了検出
- `seeked` - シーク完了検出
- `loadedmetadata` - メタデータ読み込み完了

### 定数
| 定数名 | 値 | 説明 |
|--------|-----|------|
| `SAVE_INTERVAL_MS` | 5000 | 定期保存間隔（ms） |
| `VIDEO_CHECK_INTERVAL_MS` | 100 | 動画要素チェック間隔（ms） |
| `VIDEO_CHECK_TIMEOUT_MS` | 5000 | チェックタイムアウト（ms） |
| `NOTIFICATION_DURATION_MS` | 3000 | 通知表示時間（ms） |
| `NOTIFICATION_FADE_MS` | 500 | 通知フェードアウト時間（ms） |
| `MIN_VALID_DURATION` | 1 | 最小有効動画長（秒） |
| `MIN_SAVE_TIME` | 0 | 最小保存時間（秒） |
| `DURATION_DIFF_THRESHOLD` | 5 | 動画長さ差異許容値（秒） |
| `POPSTATE_INIT_DELAY_MS` | 300 | popstate 後の初期化遅延（ms） |
| `SEEKED_TIMEOUT_MS` | 3000 | seeked イベントのタイムアウト（ms） |
| `SETTINGS_KEY` | `yt_position_settings` | 設定用ストレージキー |
